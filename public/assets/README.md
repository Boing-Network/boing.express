# Assets

- **hex-grid.svg** — Used by `.page-app` for dashboard/app background. Single hexagon outline; CSS repeats it at 60×60px.
- **boing-aquatic-space-bg.webp** — Add the Aquatic-Space background image here for landing pages (`.page-landing`). Until added, the landing page will show the existing animated background.
